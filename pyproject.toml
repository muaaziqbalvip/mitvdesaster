[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mitv-panel"
version = "1.0.0"
dependencies = [
    "fastapi",
    "firebase-admin",
    "bcrypt",
]

[tool.vercel]
entrypoint = "api/get.py"
